{#
  constraint-kit renderer: active-task.j2
  ────────────────────────────────────────
  Renders a portable active-task.md for the Google Drive path.
  This file is attached to Gemini/Claude browser sessions.

  Same input context variables as session-prompt.j2.
#}
# {{ project }} — Active Task
Last updated: {{ render_date }}

---

## Project
**Name:** {{ project }}
**Role:** {{ role.name }}

## Current Task
{{ task }}

## Current Mode
{{ mode }}
{%- if doc_type %}
Document type: {{ doc_type }}
{%- endif %}

## Active Skills
The following skills are loaded for this session:
{%- for skill in skills %}
- **{{ skill.name }}**: {{ skill.description }}
{%- endfor %}

{%- if recommended %}

## Recommended Skills
These are available but not required. Confirm with the user before applying:
{%- for skill in recommended %}
- **{{ skill.name }}**: {{ skill.description }}
{%- endfor %}
{%- endif %}

{%- if context %}

## Project Background
{{ context }}
{%- endif %}

## Session History
{%- if session_history %}
{%- for entry in session_history %}
- **{{ entry.date }}**: {{ entry.note }}
{%- endfor %}
{%- else %}
_(no previous sessions)_
{%- endif %}

---

## Mode Switch Snippets
Paste the relevant block when you change modes mid-session.

### → Reasoning mode
```
Mode: reasoning
I need you to help me think through a problem.
Apply constraint-kit reasoning skills: surface assumptions,
explore alternatives, do not recommend until options are explored.
Active skills: {{ skills | selectattr('modes', 'containing', 'reasoning') | map(attribute='name') | join(', ') or 'none for this role' }}
```

### → Collaborating mode
```
Mode: collaborating
I need us to work through this together interactively.
Ask one question at a time. Summarize before proceeding.
Do not generate output until the goal is confirmed.
Active skills: {{ skills | selectattr('modes', 'containing', 'collaborating') | map(attribute='name') | join(', ') or 'none for this role' }}
```

### → Generating mode
```
Mode: generating
I need you to produce output now.
Apply all active skill constraints before writing.
Produce output section by section. Confirm each before continuing.
Active skills: {{ skills | selectattr('modes', 'containing', 'generating') | map(attribute='name') | join(', ') or 'none for this role' }}
```

---
_Generated by constraint-kit. Re-generate when task or mode changes._
